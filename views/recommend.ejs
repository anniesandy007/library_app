<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Book Recommendations</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        body { padding: 50px; }
        .book-card { margin-bottom: 20px; }
    </style>
</head>
<body>
    <div class="container">
        <h1 class="mb-4">Find Your Next Read</h1>
        <p>Describe the kind of book you're looking for, and we'll find recommendations for you!</p>
        
        <form action="/recommend" method="POST">
            <div class="form-group">
                <input type="text" name="prompt" class="form-control" placeholder="e.g., 'a story about space travel and aliens'" required>
            </div>
            <button type="submit" class="btn btn-primary">Find Books</button>
        </form>

        <hr class="my-5">

        <% if (typeof books !== 'undefined' && books.length > 0) { %>
            <h2>Recommendations</h2>
            <div class="row">
                <% books.forEach(function(book) { %>
                    <div class="col-md-4">
                        <div class="card book-card">
                            <div class="card-body">
                                <h5 class="card-title"><%= book.title %></h5>
                                <h6 class="card-subtitle mb-2 text-muted"><%= book.authors %></h6>
                            </div>
                        </div>
                    </div>
                <% }); %>
            </div>
        <% } %>
    </div>
</body>
</html>